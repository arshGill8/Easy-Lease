# Path (relative to server/) to the fillable PDF lease template
PDF_TEMPLATE_PATH=public/lease_doc.pdf

# Origin allowed by CORS (e.g. http://localhost:5173 in dev, the deployed client URL in prod)
ALLOWED_ORIGIN=http://localhost:5173

# Port the API listens on (optional; defaults to 3001)
PORT=3001
